---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cpanel_git_repository Resource - terraform-provider-cpanel"
subcategory: ""
description: |-
  Manages one cPanel Git Version Control repository.
---

# cpanel_git_repository (Resource)

Manages one cPanel Git Version Control repository.

## Example Usage

```terraform
resource "cpanel_git_repository" "website" {
  name                       = "Website"
  repository_root            = "repositories/website"
  source_repository_url      = "https://github.com/example/website.git"
  delete_contents_on_destroy = false
}

# Before changing source_repository_url at the same repository_root, set
# delete_contents_on_destroy to true and apply that change separately.
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The display name of the Git repository.
- `repository_root` (String) The normalized repository directory relative to the cPanel account home.

### Optional

- `delete_contents_on_destroy` (Boolean) Whether destroying the Terraform resource permanently deletes the repository directory and all contents. Defaults to `false`, which removes only the Terraform state and leaves the cPanel-managed repository intact.
- `source_repository_url` (String, Sensitive) The HTTPS or SSH URL cloned when cPanel creates the repository. Changing it at the same `repository_root` requires `delete_contents_on_destroy` to already be enabled in state.

### Read-Only

- `absolute_repository_root` (String) The absolute repository path reported by cPanel.
- `available_branches` (Set of String) The branches reported by cPanel.
- `branch` (String) The currently checked-out branch, when present.
- `deployable` (Boolean) Whether cPanel reports that the repository can be deployed.
- `read_only_clone_urls` (Set of String) The read-only clone URLs reported by cPanel.
- `read_write_clone_urls` (Set of String) The read-write clone URLs reported by cPanel.
- `source_repository_name` (String) The source remote name reported by cPanel, usually `origin`.
- `type` (String) The version-control type reported by cPanel. Managed repositories use `git`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import cpanel_git_repository.website repositories/website
```
